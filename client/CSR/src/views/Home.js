import React from 'react';
import Layout from '../components/Layout';
import { API_DOMAIN } from '../config';

const Home = () => {
	return (
		<React.Fragment>
			<Layout>
				<h1>Home</h1>
			</Layout>
		</React.Fragment>
	);
};

export default Home;
